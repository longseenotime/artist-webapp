<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - Artist Studio</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="min-h-screen bg-stone-warm-50 flex items-center justify-center">
    <div class="max-w-md w-full space-y-8 p-8">
        <div class="text-center">
            <div class="flex items-center justify-center space-x-2 mb-8">
                <div class="w-10 h-10 bg-sage-600 rounded-lg flex items-center justify-center">
                    <span class="text-white font-bold text-lg">A</span>
                </div>
                <h1 class="font-serif font-semibold text-2xl text-stone-warm-900">Artist Studio Admin</h1>
            </div>
            <p class="text-stone-warm-600">Please sign in to manage your paintings</p>
        </div>
        
        <div class="bg-white rounded-xl shadow-sm border border-stone-warm-100 p-8">
            <% if (error) { %>
            <div class="mb-6 p-4 bg-red-50 border border-red-200 rounded-lg">
                <p class="text-red-800 text-sm"><%= error %></p>
            </div>
            <% } %>
            
            <form method="POST" action="/admin/login" class="space-y-6">
                <div>
                    <label for="username" class="block text-sm font-medium text-stone-warm-700 mb-2">
                        Username
                    </label>
                    <input type="text" 
                           id="username" 
                           name="username" 
                           required
                           class="w-full px-4 py-3 border border-stone-warm-200 rounded-lg focus:ring-2 focus:ring-sage-500 focus:border-sage-500 transition-colors">
                </div>
                
                <div>
                    <label for="password" class="block text-sm font-medium text-stone-warm-700 mb-2">
                        Password
                    </label>
                    <input type="password" 
                           id="password" 
                           name="password" 
                           required
                           class="w-full px-4 py-3 border border-stone-warm-200 rounded-lg focus:ring-2 focus:ring-sage-500 focus:border-sage-500 transition-colors">
                </div>
                
                <button type="submit" 
                        class="w-full btn-primary">
                    Sign In
                </button>
            </form>
            
            <div class="mt-6 pt-6 border-t border-stone-warm-100 text-center">
                <p class="text-sm text-stone-warm-500">
                    Demo credentials: admin / admin123
                </p>
            </div>
        </div>
        
        <div class="text-center">
            <a href="/" class="text-sage-600 hover:text-sage-700 text-sm transition-colors">
                ← Back to Website
            </a>
        </div>
    </div>
</body>
</html>